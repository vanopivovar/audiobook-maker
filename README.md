# 📚 Audiobook Maker

Веб-приложение для конвертации текста в аудиокниги на русском языке.

**Движок:** Silero TTS v5
**Возможности:**
- 🎙️ 5 профессиональных голосов (мужские и женские)
- 🎧 Предпрослушивание голосов перед озвучиванием
- ⚡ Автоматические ударения и омографы
- 💿 Экспорт MP3/WAV/OGG с ID3-тегами
- 🌙 Темная тема интерфейса

---

## 🐳 Запуск через Docker (рекомендуется)

### Docker Compose (самый простой способ)

```bash
# Клонируйте/скопируйте проект и запустите:
docker compose up -d

# Приложение доступно по адресу:
# http://localhost:7860
```

Готовые аудиофайлы сохраняются в папку `./output` на хосте.

### Docker вручную

```bash
# Сборка образа
docker build -t audiobook-maker .

# Запуск контейнера
docker run -d \
  --name audiobook-maker \
  -p 7860:7860 \
  -v $(pwd)/output:/app/output \
  audiobook-maker

# Остановка
docker stop audiobook-maker
```

### Настройка ресурсов

```bash
# Больше потоков CPU (по умолчанию 4):
docker run -e TTS_THREADS=8 ...

# Ограничение памяти:
docker run --memory=4g ...
```

---

## 🖥 Запуск без Docker

```bash
# Виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# Зависимости
pip install -r requirements.txt

# Для MP3 экспорта нужен ffmpeg:
# Ubuntu:  sudo apt install ffmpeg
# Mac:     brew install ffmpeg
# Windows: https://ffmpeg.org/download.html

# Запуск
python app.py
# → http://localhost:7860
```

---

## ✨ Основные функции

### 🎧 Предпрослушивание голосов
Нажмите кнопку "🎧 Прослушать голос" чтобы услышать пример выбранного диктора перед озвучиванием

### ⚙️ Гибкие настройки
- Скорость речи (0.5x - 2.0x)
- Пауза между предложениями (0.1 - 2.0 сек)
- Выбор формата и качества аудио# 📚 Audiobook Maker

Веб-приложение для конвертации текста в аудиокниги на русском языке на
базе **Silero TTS v5**.

Проект ориентирован на локальный запуск без GPU и позволяет
синтезировать длинные тексты и книги с минимальной нагрузкой на систему.

------------------------------------------------------------------------

## ✨ Возможности

-   🎙️ 5 голосов Silero TTS (мужские и женские)
-   🎧 Предпрослушивание диктора перед синтезом
-   📚 Потоковый синтез длинных текстов (без накопления в RAM)
-   ⚡ Автоматические ударения и омографы
-   💿 Экспорт MP3 / WAV / OGG с ID3-тегами
-   🌙 Тёмная тема интерфейса
-   🐳 Полностью docker-ready

------------------------------------------------------------------------

## 🧠 Архитектура

Приложение построено по модульному принципу:

-   `tts_model` --- загрузка и инициализация Silero TTS
-   `text_processing` --- предобработка текста
-   `synthesizer` --- потоковый синтез речи
-   `ui` --- интерфейс Gradio
-   `converters` --- импорт файлов

Синтез выполняется стримингом на диск, что позволяет работать с длинными
книгами без переполнения памяти.

------------------------------------------------------------------------

## 🐳 Быстрый запуск через Docker (рекомендуется)

### Docker Compose

``` bash
docker compose up -d
```

Приложение будет доступно:

    http://localhost:7860

Готовые аудиофайлы сохраняются в папку `./output`.

------------------------------------------------------------------------

### Docker вручную

``` bash
docker build -t audiobook-maker .

docker run -d \
  --name audiobook-maker \
  -p 7860:7860 \
  -v $(pwd)/output:/app/output \
  audiobook-maker
```

Остановить контейнер:

``` bash
docker stop audiobook-maker
```

------------------------------------------------------------------------

### Настройка ресурсов

``` bash
# Количество потоков CPU
docker run -e TTS_THREADS=8 ...

# Ограничение памяти
docker run --memory=4g ...
```

------------------------------------------------------------------------

## 🖥 Запуск без Docker

``` bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

pip install -r requirements.txt
```

Для экспорта MP3 требуется ffmpeg.

Ubuntu:

    sudo apt install ffmpeg

Mac:

    brew install ffmpeg

Windows:

    https://ffmpeg.org/download.html

Запуск:

``` bash
python app.py
```

------------------------------------------------------------------------

## ⚙️ Основные функции

### 🎧 Предпрослушивание голосов

Позволяет услышать пример выбранного диктора перед генерацией аудио.

### 📚 Потоковый синтез длинных текстов

Синтез выполняется без накопления аудио в оперативной памяти.

-   подходит для длинных книг
-   стабильная работа на слабых машинах
-   предсказуемое потребление ресурсов

### 🧠 Интеллектуальная обработка текста

Перед синтезом текст проходит предобработку:

-   нормализация переносов
-   корректная сегментация предложений
-   адаптация пунктуации под TTS

### ⚙️ Гибкие настройки

-   Скорость речи (0.5x -- 2.0x)
-   Пауза между предложениями
-   Формат и качество аудио
-   ID3-теги (название, автор)

### ⚡ Работа без GPU

Silero TTS оптимизирован под CPU и не требует видеокарты.

------------------------------------------------------------------------

## 🎙 Голоса

  Имя        Пол       ID
  ---------- --------- ---------
  Ксения     Женский   xenia
  Байя       Женский   baya
  Ксения 2   Женский   kseniya
  Айдар      Мужской   aidar
  Евгений    Мужской   eugene

------------------------------------------------------------------------

## 💿 Форматы экспорта

  Формат         Описание
  -------------- ---------------------------
  MP3 128 kbps   Компактный размер
  MP3 192 kbps   Баланс качества и веса
  MP3 320 kbps   Максимальное качество MP3
  WAV            Без сжатия
  OGG Vorbis     Открытый формат

MP3 поддерживает ID3-теги.

------------------------------------------------------------------------

## 📝 Управление ударениями

Модель автоматически обрабатывает омографы.\
Для ручной коррекции используйте `+` перед ударной гласной:

    зам+ок   → замо́к
    з+амок   → за́мок
    м+ука    → му́ка
    мук+а    → мука́

------------------------------------------------------------------------

## 🏗 Структура проекта

    audiobook_maker/
    ├── app.py
    ├── config.py
    ├── tts_model.py
    ├── text_processing.py
    ├── synthesizer.py
    ├── ui.py
    ├── converters.py
    ├── Dockerfile
    ├── docker-compose.yml
    ├── requirements.txt
    └── output/

------------------------------------------------------------------------

## 📄 Поддерживаемые форматы

  Формат   Поддержка
  -------- -------------
  .txt     ✅
  .md      ✅
  .docx    ✅
  .pages   ⚠️ Частично

------------------------------------------------------------------------

## ⚙️ Системные требования

  Параметр   Минимум        Рекомендуется
  ---------- -------------- ---------------
  Python     3.10+          3.11+
  RAM        2 GB           4 GB
  CPU        2 ядра         4+ ядра
  GPU        Не требуется   ---

------------------------------------------------------------------------

## 📄 Лицензия

Silero TTS v5_ru --- **CC-NC-BY**\
Для коммерческого использования: hello@silero.ai

- ID3-теги (название, автор)

---

## 🎙 Голоса

| Имя       | Пол     | ID      |
|-----------|---------|---------|
| Ксения    | Женский | xenia   |
| Байя      | Женский | baya    |
| Ксения 2  | Женский | kseniya |
| Айдар     | Мужской | aidar   |
| Евгений   | Мужской | eugene  |

---

## 💿 Форматы экспорта

| Формат          | Описание                        |
|-----------------|---------------------------------|
| MP3 128 kbps    | Компактный, для мобильных       |
| MP3 192 kbps    | Оптимальный баланс (по умолч.)  |
| MP3 320 kbps    | Максимальное качество MP3       |
| WAV             | Без сжатия, для постобработки   |
| OGG Vorbis      | Открытый формат, хорошее сжатие |

MP3-файлы поддерживают ID3-теги: название, автор (задаются в настройках экспорта).

---

## 📝 Управление ударениями

Модель автоматически расставляет ударения и обрабатывает омографы. Для ручной коррекции ставьте `+` перед ударной гласной:

```
зам+ок   → замо́к (дверной)
з+амок   → за́мок (крепость)
м+ука    → му́ка (страдание)
мук+а    → мука́ (пшеничная)
```

---

## 🏗 Структура проекта

```
audiobook_maker/
├── app.py              # Точка входа (запуск приложения)
├── config.py           # Константы и настройки
├── tts_model.py        # Загрузка модели Silero TTS
├── text_processing.py  # Обработка текста (разбивка, предобработка)
├── synthesizer.py      # Ядро синтеза речи
├── ui.py               # Gradio-интерфейс
├── converters.py       # Конвертация форматов (.txt, .docx, .pages)
├── Dockerfile          # Docker-образ
├── docker-compose.yml  # Docker Compose конфигурация
├── .dockerignore       # Исключения для Docker
├── .gitignore          # Исключения для Git
├── requirements.txt    # Python-зависимости
├── README.md           # Документация
└── output/             # Выходные аудиофайлы
```

## 📄 Поддерживаемые форматы

| Формат | Поддержка | Примечания |
|--------|-----------|------------|
| .txt   | ✅ Полная | Текстовые файлы (UTF-8, CP1251, CP866) |
| .md    | ✅ Полная | Markdown файлы |
| .docx  | ✅ Полная | Microsoft Word документы |
| .pages | ⚠️ Частичная | Только старый формат (до Pages 5.0) |

**Для .pages файлов нового формата:**
Экспортируйте файл: `Файл → Экспортировать → Word (.docx)` или `Обычный текст (.txt)`

---

## ⚙️ Системные требования

| Параметр    | Минимум          | Рекомендуется     |
|-------------|------------------|-------------------|
| Python      | 3.10+            | 3.11+             |
| RAM         | 2 GB             | 4 GB              |
| CPU         | 2 ядра           | 4+ ядра           |
| Диск        | 1 GB             | 2 GB              |
| GPU         | Не требуется     | —                 |
| Docker      | 20.10+           | 24.0+             |

---

## 📄 Лицензия

Модель Silero TTS v5_ru: **CC-NC-BY** (бесплатно для некоммерческого использования).  
Для коммерческого использования: hello@silero.ai
